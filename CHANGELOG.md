# 更新日志

本文件记录 `kv-ui-theme-adapter` 的行为规则、执行工具和验收能力变化。每次修改 Skill 的受控文件时，必须在同一变更中更新本文件；最新记录置顶。

## 2026-09-23

### 新增

- 增加本更新日志、日志维护规则、Git 变更检查脚本和 GitHub 自动检查。
- 增加 KV 锁定子树、快速基线传输、区域化写入、无损颜色解绑及副本投影审计工具。
- 增加跨渐变、标题多层 Paint、卡顶多氛围层及其他材质成员的 `materialRelationPlan` 与确定性色彩关系塌缩审计。
- 增加经过代表门禁、写集独立性与代码预算约束的多区域 Figma 批处理生成器。

### 调整

- 统一全页家族配方与权威计划，阶段使用派生切片；等价实例复用决策，非等价宿主/状态补充代表试色。
- 取消独立区域之间逐卡等待视觉 PASS 的串行约束，保留每批即时检查，集中逐实例审查并按共同原因修复。
- 最终审计复用依赖未变的局部证据，保留最终全树及整页检查；性能目标不作为质量或修正次数上限。
- 浅色 UI 的普通主文案改为低 B 深色基色，默认 `B 18–32`；深色 UI 的普通文字基色保持 `B=100`。
- 无金属撞色优先比较约 180° 对向色与 KV 中更鲜明的高彩高明度色。
- 浅色撞色卡片允许使用低彩近白或纯白稳定底，只在已有顶部氛围中保留主题色。
- 特别鲜艳的撞色按钮优先使用白字；其他按钮按真实材质和宿主决定字色。
- 卡片标题与功能 Icon 增加逐实例本体覆盖检查，防止只修改底色或底托。
- Icon 渐变必须逐色标保留原有职责差异，禁止把不同端点映射成同一颜色。
- 六色方案明确为稳定锚点而非颜色桶；标题、Icon、按钮与卡片氛围的派生层不得机械映射成同一 RGB。
- 不改变 Paint 拓扑时默认只传颜色叶子原子 diff，避免完整 Paint 数组反复传输；多区批处理遇到任何审查状态立即停止。
- 新 KV 作为只读视觉资产直接使用，写入计划必须排除其完整子树。
- 禁止在不可编辑位图上新增按钮、覆盖层或重画文案来冒充换色完成。

### 验证

- 本次流程调整完成入口、扩展阶段与批处理规则的交叉检查，取消逐卡等待的冲突要求；未运行 Figma 性能实测。当前 quick_validate 因缺少 PyYAML 未完成，不使用替代解析器冒充完整校验；日志检查因无 Git 元数据跳过变更集检查。
- Python 118 项、JavaScript 32 项通过，覆盖新增关系审计及多区域批处理守卫。
- Skill 结构校验通过。

## 2026-09-21

- 扩展 KV 取色、配色提案、Figma 写入保护、材质审计和完整页面验收规则。
- 增加按钮、Icon、Tab、文字、卡片氛围及视觉单元的一致性检查。

## 2026-09-17

- 创建 `kv-ui-theme-adapter` Skill。
- 建立原稿保护、页面复制、KV 替换、主题换色和基础审计流程。
