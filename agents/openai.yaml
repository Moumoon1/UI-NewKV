interface:
  display_name: "KV UI 换肤"
  short_description: "先提供含 Icon 的六色方案，确认后完整替换 KV 并换肤验收"
  default_prompt: "使用 $kv-ui-theme-adapter，先分析我提供的新 KV 完整视觉单元与原页面，并给出背景、卡片、数字、Icon、按钮、Tab 六色方案；等我选定方案后，再复制活动页副本、保持 KV 原尺度与内部渐变、保护独立营销 Banner，并完成试色、换肤和验收。"
policy:
  allow_implicit_invocation: true
