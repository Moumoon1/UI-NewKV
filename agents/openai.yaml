interface:
  display_name: "KV UI 换肤"
  short_description: "先提供新 KV 配色方案，确认后完成活动页换肤与验收"
  default_prompt: "使用 $kv-ui-theme-adapter，先分析我提供的新 KV 与原页面并给出规定的完整配色方案；等我选定方案后，再复制活动页副本、替换 KV，并完成试色、换肤和验收。"
policy:
  allow_implicit_invocation: true
