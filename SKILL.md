---
name: kv-ui-theme-adapter
description: 根据用户提供的新 KV，先分析原 KV、原 UI 与新 KV 的深浅，从新 KV 提炼环境色、材质色和主视觉色相；无金属证据时提供同色系/撞色方案，有可靠金属证据时提供同色系金属、明亮金色和干净深色按钮方案，并以背景、卡片、数字、Icon、按钮、Tab 六色供确认；确认后再为选中的 Figma 顶层活动页复制副本、完整替换 KV 视觉单元、完成换肤及真实渲染验收。
---

# KV 活动页 UI 换肤

先分析原 KV、原 UI 和新 KV 的深浅及新 KV 的环境色、材质色与主视觉色相。**初始选色必须先定表面家族，再定明度拓扑和彩度/受光目标，最后才生成 Hex 与方案预览**；任何表面跨家族、暗沉、灰闷或只靠局部高光伪装明快的方案，必须在展示给用户前淘汰。`targetUiMode=light` 时，页面背景保留可识别的主题色，不得淡到近白或灰雾；卡片可更接近白色，以页面↔卡片的明度和彩度差突出卡片。主要操作必须有足够有效彩度并与真实宿主清楚分离。没有可靠金属证据时比较同色系与色环撞色；有可靠金属证据时，同一 UI 模式固定给出同色系金属、正常明亮金色和干净深色三种按钮方向。金色方案优先从类似 `#FFEED7→#FFC58A`、`#FFEEC6→#F9DB92`、`#FFE5A3→#FFC165` 的暖亮范围按 KV 微调，保持高明度、有效彩度与清楚受光，不用暗铜、棕橙或土黄代替，并默认配深棕字；深色按钮独立成案，不能把金色压暗后冒充金色方案。通过初始选色门禁后，按新 KV 明暗生成规定的完整配色方案供用户选择；用户确认方案后，再把新 KV 按原设计尺寸或页面同宽尺度放入活动页副本的 Hero，并调整颜色、渐变和已有材质层。默认保持完整 KV 视觉单元的尺度和内部构图，超出 Hero 的下方高度允许截去，不能为完整显示尾部而整体缩小；只有用户明确要求整图完整显示时使用 contain。必要补底来自新 KV 稳定环境。原稿必须保持不变，结果放在原稿右侧。

本 Skill 不是固定 Hue 映射器。像素与亮度提供证据；对应关系的对比度按同模式原稿基线或跨模式/画风迁移的情境策略验收；AI 必须理解新 KV 的环境、主体、材质、光照、颜色面积与业务层级，并通过真实页面试色决定最终方案。

按钮、大按钮（CTA）和功能 Icon 必须在真实背景上突出；CTA 是最强操作焦点，卡片内主要按钮局部醒目，Icon 图形清楚且不被底托吞没。主要小按钮与 CTA 默认共享同一个 `buttonPrimary` 代表色和主体色相/中间调，差异由原有尺寸、光影和结构表达；不得把同一主要操作家族分别做成黄色、橙色或其他可见异色。Figma 的 `B` 等同 HSV 的 `V`；按钮 HSB 只按页面模式和尺寸分为 `深色 UI 小按钮 / 深色 UI 大按钮 / 浅色 UI 小按钮 / 浅色 UI 大按钮` 四类，不能再把按钮自身的“浅色/深色”误写成四类。普通鲜亮彩色按钮中，所有承担主体本色且有可见面积的色标以 `B=100` 为默认门禁；用户指定的暖亮金示例允许 `B98–100` 的近满明度色标，但不因此归入低 B 方向。除该金色近满明度容差外，`B<100` 只作为两类有证据的例外：深色 UI 中有可靠金属证据的金属主体/金属深端分段，以及浅色 UI 中为浅页面建立操作分离的 `clean-deep` 深色按钮。渐变不自动授权把稳定暗端降到 `B77/80/90`。白色高光、透明端、极小阴影和结构层不参与主体 B 门禁。小按钮与大按钮分别总结并 PASS 后，再检查共同锚点与家族一致性；不得用 CTA 通过覆盖小按钮失败。

独立营销 Banner 的创意背景、插画、图片与营销文案可以保持原样，但其中可单独识别、可编辑且承担点击/提交/参与语义的按钮或页面级 CTA 不属于创意保护范围；它继续归入主要按钮家族，并与小按钮共同引用 `buttonPrimary`。只有 CTA 已烘焙进不可编辑的单张图片，或用户明确要求整个 Banner 连同操作控件都保持原样时，才整体保护。不能因按钮嵌套在 Banner 实例内就漏掉大按钮换肤。

一般主要彩色按钮的文字默认先使用纯白或与 KV 冷暖协调的近白，并让小按钮与 CTA 共用同一文字方向。只有按钮在最终文字覆盖区域确属高明度浅色、白/近白字无法清楚阅读，且在不破坏已选 `buttonPrimary`、明快感和材质的前提下无法合理调整主体时，才允许以真实对照证据改用深色字；不能因为深色字对比度数值更高或看起来“稳妥”就跳过白字方案。`gold-bright` 明亮金色按钮是明确例外，默认使用深棕字，并在最亮、最暗和实际文字覆盖区验证；不得为保白字而压暗金色。明确的禁用按钮不另选灰色、米色或低 B 配方：先完整复用同族正常按钮的 Fill、Stroke、Effect 与字色，再在最小完整按钮视觉单元上应用相对正常态 `50%` 的整体有效透明度；若原状态已有等效 50% 衰减则不得重复相乘。次级按钮与业务语义不同的浅底按钮另行分类。

Icon 图形、按钮主体和普通辅助 Icon 底托分别配色。六个公开锚点是六种角色，不是六个互不相关的强调色。每套方案展示前冻结 `accentFamilyPlan`：普通功能 Icon 的稳定主体仍从背景/卡片的连续环境家族提取并提亮、增彩，不从按钮或数字倒推色相；`targetUiMode=dark` 时，高亮数字的公开 `number` 锚点与 `icon` 稳定主体锚点必须是同一个 Hex/RGB，按钮可以作为第二个强调家族。浅色 UI 的数字可按宿主独立选择，但数字、Icon、按钮默认仍不超过两个具有显著面积的高彩家族。尤其不得仅因 KV 有一小块蓝青反光，就在紫色数字、蓝色按钮之外再发明一组高亮青蓝 Icon；局部金属反射可以借用少量青蓝光色，但不能成为 Icon 稳定主体。普通鲜亮 Icon 的主体本色仍优先使用 `B=100`，较低 B 只留给可解释的小面积塑形暗部或经 KV 材质证据确认的金属/深色 Icon，不得把整组 Icon 压成低明度。`icon` 不等于底托。底托按第 7.1 节独立选色，弱于图形，但必须淡而可见，在 1:1 下即使忽略图形也能与卡片分离，不能因浅色再叠极低 Alpha 而消失。默认保留渐变空间结构和材质分工；已有明确 Alpha 例外按对应规则执行。原 S/V 只可作为第 3.2 节浅→浅分支的初始比较基线，或在明确通道锁定中保持，不能扩展为所有模式的限制；精确保护项仍保留。

原稿功能 Icon 若依赖多色标金属/镜面层，`icon` 六色锚点只锁定稳定主体色标，不代表把整条渐变刷成该色。Phase 3 逐层登记可见的基底、暗端、反射和高光目标；Phase 5 代表试色及 Phase 7 全实例用 `scripts/material_stop_audit.py` 检查原本有色差的可见渐变是否被压成同一 RGB，并看 1:1 原稿/副本对照。任一材质层塌缩即先修副本 Icon、重验其视觉单元，不能以 Paint 数量、渐变方向或六色锚点通过替代材质通过。

`targetUiMode=light` 时必须把卡片氛围、Icon 材质和按钮材质中的黑色/近黑色、无彩低明度端点、暗色 Effect 与主题装饰位图纳入可见贡献审计；旧主题残留不只包括旧 Hue。对每个卡顶光影/形状先作 `keep / fade / remove-visually` 判断：只有仍能解释新 KV 环境光且不会制造顶部暗块的层才保留；保留层重着色为非常淡的环境邻近色，冗余的纯装饰氛围允许把既有 Paint/色标 Alpha 降到 `0` 以视觉移除，但不删除节点、不改几何/蒙版/层级，也不归零稳定卡片底色。结构蒙版或 Boolean 几何可保留黑色，任何实际输出像素的卡顶氛围都不得形成暗了一块、灰带、厚重光斑或高频形状。功能 Icon 的主体、中间调和大面积塑形暗部同样不能落成黑灰或低明度脏色；只允许为立体轮廓保留小面积、可解释的局部暗部，最终图形仍须明亮清楚。

普通卡片标题默认使用与正文主色一致的 `textBaseColor`，通过原字号、字重和位置建立层级；不因页面存在金属 KV、金色按钮或金色数字就自动改成金色。只有原稿已经把标题定义为独立强调角色，或存在明确业务/主题证据时，才登记 `titleAccent` 例外；强调数字继续使用独立的 `dataAccent`，标题不得机械复用。

卡片顶部的包边、角饰、标题装饰线、顶部光带和其他卡片 Chrome 默认属于 `cardAtmospherePalette` / 表面主色系，从背景、卡片及其邻近环境色派生；不自动继承 `dataAccent`、Icon 或 `buttonPrimary`。除非原稿已把该装饰定义为明确业务强调且新主题仍有对应证据，不能因为本页使用金色数字或按钮就给每张卡顶部加一圈黄色/金色。

先看原稿真实灰度图与业务语义，确定信息层级，再建立新主题的目标明暗关系和颜色。灰度分析必须形成元素间可复验的关系，不能只生成图片和角色描述：同族主要小按钮与 CTA 的主体灰度、色彩和材质重量接近，变化主要由已有光影、渐变和尺寸表达；按《颜色决策规则》第 1、8 节执行。保留信息优先级，不机械照搬绝对灰度；跨深浅模式允许整个家族反转明暗方向，不能各自独立反转。

颜色一致性分别检查等价实例/连续表面的新配方一致与 KV→UI 环境延续。页面背景、卡片和容器从新 KV 的稳定环境家族派生；选择主表面锚点时，大面积连续环境优先于局部前景物，不按单个色块或底部位置机械决定。实际 Hero 可见下缘若是横向连续、面积充足、颜色较纯且可自然延展的环境场（如草原、海面、天空或雾光），可作为页面背景延伸的重要候选；若只是树叶、建筑、人物、阴影、装饰带或被裁掉的区域，则不能据此决定整页背景。没有可靠有彩环境家族时，使用有证据的冷暖中性方向并降低色相结论的可信度。同色系方案的交互与强调从环境、主体或材质的同家族/邻近家族派生。主体或标题存在可靠、显著的金属视觉时，不再用一个“金属撞色”方案包办：同一 UI 模式分别生成本次金属家族的同色系金属按钮、正常明亮金色按钮和有来源的干净深色按钮。银色受蓝色环境反射而形成银蓝灰时，它仍属于同色系金属，不因更深就称为撞色。正常金色方案必须清楚、明亮、有彩，禁止以暗铜、棕橙、土黄或低明度黄褐替代；深色方向单独比较，不覆盖金色方案。没有可靠金属证据时，再以表面主家族为色环基准生成真正可见分离的对向撞色，不能把同一色相的深浅变化标为撞色。主要小按钮与 CTA 始终共用同一按钮锚点。Tab 选中态默认仍归属页面/卡片的同色系，只用明度、彩度、边界或指示条表达状态，不跟随按钮方向变化。大面积表面必须有连续的色相/冷暖关系，局部强调不得扩散为并列的大面积表面家族。具体提案矩阵和确认门禁见[《配色方案提案》](references/palette-proposal.md)，颜色来源与家族规则见《颜色决策规则》第 3.0 节，实例关系按第 9 节。

必须单独形成 `heroBottomExtensionDecision`，但不得机械锁定底部颜色。若最终 Hero 可见下缘杂乱、纹理具体、被人物/建筑/树叶/阴影/装饰等局部前景主导，底部不得作为页面背景锚点，改从整张 KV 的大范围连续环境场选择。若下缘是大片草原、海面、天空、雾光、较纯颜色或低纹理渐变，可把“从底部继续延伸”列为强候选，再与其他大范围环境色按空间合理性、整页面积效果、卡片层级和视觉舒适度比较；满足条件不等于必须采用。只有最终决定延伸时，接缝起点才须匹配实际下缘，并按原空间关系继续纯色或渐变；决定不延伸时须记录更合适的环境依据及既有/允许的衔接方式，不能留下无解释的生硬断层。

当页面同时存在这三类角色时，无论深浅模式，颜色显著性都必须按 `Icon 底托 < 选中 Tab < 主要按钮/CTA` 依次增强。大小主要按钮共用 `buttonPrimary`，成为三者中最明确的操作色，CTA 的额外强化只来自原有尺寸和材质。这个顺序看最终合成后的色彩存在感、宿主分离、面积和边界，不要求某个单一明度或彩度数值机械递增。`targetUiMode=light` 时，Icon 底托只需淡淡带有表面家族的颜色；Tab 仍属于表面同系，但要比 Icon 底托更容易感知选中状态；二者都不能暗沉，也不能形成灰粉、灰蓝或近黑的沉重色块。若顺序倒置、彼此近似或形成高频暗点，须回到真实宿主调整配方后再验。

用户选定的六色方案是精确执行契约，不是仅供参考的色相建议。Phase 3 必须为 `background/card/number/icon/button/tab` 各指定至少一个稳定可见的 `anchorId` 通道，目标 RGB 与 `schemeAnchorContract` 对应值一致；派生暗部、高光和反射只能服务该锚点，不能把最终稳定主体改成另一代表色。Tab 尤其不得因执行方便改用按钮色或 Icon 色。代表试色写入后、全页扩展前以及 Phase 7 都须读回六个锚点；任何缺失或不一致均为 FAIL，必须回到配方或重新向用户展示方案，不能静默漂移。

普通标签和 Tab 文案不是 `text.muted`。交互标签、选中 Tab 的文字默认使用 `100%` 有效文字强度；未选 Tab 和承载有效信息的普通标签从 `80%` 起验证，只有原稿明确属于弱提示且 1:1 仍清楚时才可降到 `60%`，不得落入 `40%` 弱化档。标签底板必须在忽略文字时仍能与卡片分离；它可弱于选中 Tab，但不能像水印一样消失。媒体封面内受保护标签继续保持原色，不受此规则影响。

同族控件同时检查跨区域一致性：内容卡和榜单卡的同层级同状态 Tab 优先共用新配方，不能仅因位于不同卡片独立调色；完整的同族比较进入 `control-family-consistency` 门禁。变体证据与范围按《颜色决策规则》第 9 节登记。

原稿中基础表面同色或接近的收益卡、任务卡、作品卡，换肤后延续相近底色，差异主要由已有光影、反射和渐变表达；收益数据的强调不自动授权整张收益卡改成强反差深色。具体按《颜色决策规则》第 4 节检查基础表面与氛围分工。

材质名称不预设 Hue；Icon 与按钮的材质感由有依据的基底、明暗分段、环境反射、高光和边缘关系共同表达，按 KV、环境和真实宿主选择颜色，联合调整 H/S/V，并保留透明度与材质结构约束。具体候选与验收只维护在《颜色决策规则》第 8 节。

颜色与材质的详细标准只维护在[《颜色决策规则》](references/color-decision-rules.md)：第 3 节规定彩度、明度和清晰度，第 6 节规定强调数字，第 7 节规定 Icon/底托独立配色，第 8 节规定按钮合成及任务级用户配色偏好的优先级。每个角色仍须达到对应标准，不以“协调、克制”代替证据。

## 换色与画风迁移的选择

先比较新旧 KV 的画风，再核对原 UI 的实际风格冲突。仅配色或 IP 变化默认普通换肤；材质、体积表现或笔触明显改变时才评估迁移，且只对原 UI 中确实冲突的部位启用受限的 `style-adaptation`，Phase 2 前读取[《画风迁移规则》](references/style-adaptation.md)，允许按白名单调整主题装饰素材、体积光影、表面材质、边缘和纹理。**原 UI 的按钮、Tab、卡片形状家族与业务结构仍保留**；不能把风格迁移理解成重新设计控件。

该模式只对 `styleAdaptationPlan` 登记的风格属性覆盖下文及参考文件中“只换颜色、素材不变、Paint 类型/数量不变、局部装饰矢量几何不变、原材质必须保留”的限制。未登记节点继续执行普通换肤规则；布局、字体、文案、原稿、媒体、前三名与独立营销 Banner 保护规则不被覆盖。仅要求分析或更新 skill 时，只读设计资料并修改 skill，不复制或修改 Figma 页面。

## 强制执行协议

规则分层：原稿、业务结构、字体/文案、精确保护、写入白名单、真实证据及本次配色方案数量/确认门禁是硬边界；文字 Alpha 档位及默认位置是可解释调整的起点。新 UI 必须在实际尺寸下可读、状态可辨、容器边界可辨；仅深→深、浅→浅且不涉及画风迁移的普通换肤，要求每个对应关系的最终合成对比度不得低于原 UI；深浅转换或画风迁移按新 KV、实际宿主、材质与业务层级确定目标，原稿比值作参考，不作为逐点硬门槛。任何模式都不能用文字通过替代底色或状态通过，可读性、操作突出、状态和边界可辨均必验。此要求优先于仅换 H、保留 S/V 的初始策略；必要时联合调整合法的背景与前景，仍保持 KV 家族、明快感和保护结构。基线建立、采样与严格比较统一按《验收规则》第 5.1 节。WCAG 固定比值仍不默认启用；用户另有明确标准时同时执行，不以追求最高比值代替审美与角色层级。

首次执行按 `Phase 0 → Phase 1P → Phase 2 → 用户选色 → Phase 1W → Phase 3 → Phase 7` 的依赖顺序推进。`Phase 1P` 是配色提案所需的精简只读审计，`Phase 1W` 是任何写入前必须完成的完整原稿基线；把完整快照、全通道、逐实例材质和对比度基线推迟到用户选色后，不表示省略它们。失败修正及中断恢复允许回到受影响阶段，再重验其下游依赖；不重跑无关已验证工作，不重新复制页面。阶段顺序是证据依赖，不要求每个只读检查单独发起工具调用。

多图层控件按完整视觉单元换肤。Phase 1 从真实承载层建立 `sourceAudit.visualUnits`，Phase 3 冻结成员、同色关系、对比关系与可比较的强弱顺序；配方按整组推导。局部修正继承同一登记表与规则，通过工具计算受影响关系闭包并使门禁失效，不能只验本次改色的节点。登记、完整上下文复验及工具格式只维护在[执行工具第 4.8 节](references/execution-workflow.md#48-视觉单元与局部修正闭包)。

- 每个 Phase 的“必须产出”和“通过条件”全部满足后，才允许进入下一阶段。
- 配色方案确认是输入充分时 UI 写入前唯一例行用户决策点；确认前只做只读分析与方案展示，不复制页面、不替换 KV、不写 UI。若生成用户要求的方案类型缺少必要输入（例如没有可靠的有彩主色却要求撞色），先把缺失基准作为输入阻塞一次性说明，不伪造颜色。方案确认后的 `preflight`、代表性试色、失败修正、全页扩展和最终审计均为内部自动门禁，不重复请求审批。
- 阶段失败时必须留在当前副本中自行修正并复审；不得以时间、上下文长度、页面复杂、组件嵌套或工具调用次数为理由省略。
- 试色范围、无改善时的重新诊断、门禁失效与有限重试，统一按[《局部试色与执行工具》](references/execution-workflow.md)执行；不能反复无依据试色或沿用过期 PASS。
- 目标/KV 不唯一或不可访问、文件无法解码、权限不足、必要写入/证据路径确实不可用，或允许例外仍无法避免破坏保护结构时，才可说明真实阻塞并请求所缺信息/授权。先完成安全核查与有依据的恢复路径；局部阻塞不停止无依赖的工作。比例差异、原稿普通文字低 Alpha、可选字体加载失败不是停止理由，文字恢复见《文字换色与恢复》。
- 不得在没有实际读取、写入或截图证据时声称已经执行、检查或通过。无法取得证据的项目按 `FAIL` 处理。
- Phase 3 先估计节点、颜色通道、写入操作及预计 Figma 调用量；大页面用《局部试色与执行工具》第 5.4 节的按字节组批与完整遍历/紧凑返回，避免固定极小批次和重复传输未变化的全树。预计耗时明显超过常规页面时先向用户说明原因与当前阶段；Phase 6 形成可看副本后及时给预览链接并继续最终审计，不把预览冒称 Phase 7 PASS。
- 在 `Phase 5` 的代表性试色通过前，禁止全页换色；在 `Phase 7` 通过前，禁止报告“换肤完成”。

按时间顺序维护 `executionPhaseLog`。最低顺序为：

```text
inspect-fast → kv-style-check → kv-analysis → palette-proposal
→ palette-selected(user) → source-audit-full → preflight → clone-and-kv
→ representative-trial → trial-review(pass)
→ expand → audit(pass)
```

## 必需能力与参考文件

本 Skill 会写入 Figma。每次调用 `use_figma` 前必须加载并遵守 `figma:figma-use`。不要加载页面搭建、组件创建或重新设计类工作流。

Phase 0 读取[《局部试色与执行工具》](references/execution-workflow.md)第 3 节和第 5.4 节，初始化本次临时目录、恢复边界与提案快速路径；用户选色后、进入 `Phase 1W` 时再读取第 4–5 节并建立完整覆盖清单，Phase 3–6 按第 1–2 节组织完整场景与有效门禁。文件已加载且未变化时复用已读内容，不为进入新阶段重读整份规则。

参考文件按阶段读取，每份文件只负责一种职责：

- Phase 1P 前读取[《Figma 执行规范》](references/figma-execution.md)第 1 节，以及[《颜色决策规则》](references/color-decision-rules.md)第 1、9 节：只建立目标、页面模式、显著性、业务优先级、连续表面和同族控件骨架。配色提案前禁止传输全量 Paint 树、完整文字清单或完整结构快照。
- 用户选色后、Phase 1W 前完整读取《Figma 执行规范》，并补齐完整 `sourceAudit`。有需换肤文字时再完整读取[《文字换色与恢复》](references/text-color-recovery.md)；存在按钮材质或卡片氛围时再完整读取[《叠层合成审计》](references/composite-layer-audit.md)。这些写入级规则不得因提案快速路径而省略。
- Phase 2 前完整读取[《KV 色彩分析》](references/kv-color-analysis.md)，并读取《颜色决策规则》第 3.0–4 节：继承 Phase 1 的显著性与关系证据，先建立单一表面家族、明度拓扑和彩度/受光目标，再生成任何方案色值。
- Phase 2 取色完成后完整读取[《配色方案提案》](references/palette-proposal.md)：无可靠金属证据时按新 KV 深浅生成 4 套或 2 套候选；有可靠金属证据时按适用 UI 模式生成同色系金属、正常明亮金色、干净深色三种按钮方向（共 6 套或 3 套）。面向用户只展示背景、卡片、数字、Icon 图形、按钮、Tab 六个冻结锚点；只有通过提案前选色门禁的方案才能展示并等待用户选定。
- 用户选定后、Phase 3 冻结配方前完整读取《颜色决策规则》其余适用章节：将已通过门禁的方案展开为文字、Icon、按钮、Tab 与卡片氛围配方，不得首次在此补做或放宽表面家族、明度、彩度与受光决策。
- Phase 3 读取[《验收规则》](references/validation.md)的检查分类与最终闸门，写入前形成适用清单；Phase 7 读取全部适用检查，验证结构、颜色、可读性、残留色与真实视觉。

## 输入与目标

接收一个 Figma Design 链接，以及一张用户提供的新 KV 图片或 Figma 中明确指定的新 KV 节点。

可选指定新 UI 模式：`requestedUiMode=light/dark/auto`，自然语言“新 UI 只看浅色／深色”即有效，无需使用字段名。用户明确限制模式时只生成该模式下的候选：无金属时为同色系/撞色两案，有可靠金属时为同色系金属/正常明亮金色/干净深色三案；未限制时，深色新 KV 默认同时提供深色和浅色 UI，浅色新 KV 默认提供浅色 UI。用户从方案中选定的模式成为最终 `targetUiMode`。模式约束页面、卡片与内容表面的整体深浅，不表示每个 Icon、按钮都必须同样深／浅，也不要求修改 KV 图片。

目标优先级：

1. 用户当前唯一选中的顶层活动页 `FRAME`。
2. 链接 `node-id` 指向的唯一页面级 `FRAME`。

目标必须直接位于 Figma `PAGE` 下，并包含可确认的旧 KV 容器和 Hero 可见窗口。以下情况禁止创建副本：链接类型不符、选中多个目标、目标不是顶层 Frame、新 KV 不可访问/不可解码、旧 KV 图片 Paint 无法唯一识别，或在 KV 适配例外之外仍必须改动页面布局、文案、字体、信息结构、共享样式/变量或主组件。

## 换肤边界

默认只允许：

- 复制一次目标 Frame，并只移动副本根 Frame 的 `x`，放到原稿右侧不碰撞的位置。
- 先识别新 KV 是单图还是由图片、矢量、文字和渐变共同组成的完整复合视觉单元；复合 KV 必须整组使用，不能只抽取内层“主图”。优先验证原尺寸/同宽直接使用；可完整适配时不缩图、不补底。超高时优先保留整组尺度与内部构图并截去下方高度；仅用户明确要求整图完整显示时以 `contain` 等比适配；功能叠层不自动扣除为留白。新 KV 自带的底部渐变保持原类型、方向、色标位置和终点，不能翻转或重建。保持 Hero 外框及下方 UI 布局，允许重设副本 KV 专用图片的 `scaleMode`、`imageTransform`、定位和尺寸；必要时增加独立主图载体与最小环境补底。具体边界和计算以《Figma 执行规范》第 5 节为准。
- 修改副本既有 Fill、Stroke、渐变色标、文字 Paint 和已有带颜色 Effect 的颜色及允许的 Paint Alpha。
- 按《文字换色与恢复》登记普通 TEXT 自身的最小 Alpha 覆盖；不修改状态/祖先透明度，不改字体排版或精确保护文字。
- 解绑妨碍完整换色或 KV 适配的副本组件实例；解绑操作本身须保持可见结构、尺寸、位置、层级、文案和素材，之后仅执行已记录的换肤/KV 适配差异。原稿实例和主组件始终受保护。
- 使用参考文件明确列出的跨模式氛围转换、浅色→深色方向性描边和收益卡底部氛围 Fill 例外。
- 对已确认的禁用按钮，在最小完整按钮视觉单元应用相对正常态 `50%` 的整体有效透明度；不得改成另一套灰/米色 Paint，也不得重复叠加既有 50% 衰减。
- 新 KV 已烘焙旧 KV 标题、Logo 或主体时，将有证据的对应 `kv-asset-subtree` 整体透明度设为 `0`；独立主图载体启用后，原 KV Paint 可归零以避免旧图透出。功能型 `hero-adjacent-ui` 不得隐藏。

除此之外，布局、尺寸、圆角、坐标、Auto Layout、约束、裁切、层级、混合模式、节点顺序、文案、字体、素材、矢量/布尔/蒙版几何、共享样式、变量和主组件均不得改变。

### 不可丢失的结构原则

- UI 颜色必须写入真正参与渲染的 `visualSurfaceCarrier`，包括异形 Rectangle、Vector、Boolean、Ellipse、Frame 及其多层 Paint；不得因为语义父 Frame 没有 Fill 就漏改，也不得给 UI 空外层新增普通矩形底色。Hero 内记录在案的 KV 主图载体和补底适用独立例外。
- 对每个相关节点建立 `paintPresenceMask`。除记录在 `kvFitPlan` 中的 KV 专属差异外，`light-to-light` 与 `dark-to-dark` 时，上色/不上色关系、Paint/Effect 类型和有效开关必须与原稿一致。
- 复合异形卡的前后表面、透明度、遮挡、混合模式和材质层职责属于结构。禁止用降低前景 Alpha 制造玻璃感。
- KV 上的返回、规则、钱包、客服、分享等功能入口属于 `protected-hero-controls`，保护位置、尺寸、形状、字体、文案、素材与状态。同模式保留全部样式；浅→深或深→浅时，允许按真实 KV 宿主调整文字及对应功能图形的前景色，必要的已有底托联动按《Figma 执行规范》第 5.4 节登记；不迁移画风、不新增入口底板，不扩大到倒计时、周期或普通信息带。
- 视频封面内部左上角标签、左下角标签、播放量及其图标属于精确的 `protected-media-overlay`，完全保持原色；封面外标题、副文案和普通标签仍必须换肤。
- 排行榜前三名的名次文字和对应形状属于 `protected-rank-top3`，与视频标签一样保留原 UI 配色，包括名次徽章/奖牌、底托/领奖台、描边、高光与附属装饰。保护须精确到节点或 Paint/文字区间，姓名、收益、榜单标题、外卡片和 Tab 不随之整组跳过；详细边界见《Figma 执行规范》第 3.1 节。
- 页面中独立存在、由定制插画/图片背景与促销文案共同构成且不承担通用卡片/列表/任务/奖励结构的营销 Banner，把创意背景、插画、图片和营销文案登记为 `protected-marketing-banner` 并保持原样；不得换色、滤镜染色或纳入旧色残留清理。Banner 内或紧邻 Banner 的独立可编辑 CTA、按钮和其他通用操作控件须从保护集合中剔除并继续换肤；只有烘焙在单图中的操作画面或用户明确要求整体保留时例外。必须结合语义、构图、比例和截图判断，不能只凭名称保护普通业务卡片或漏掉 CTA。
- 成功、警告、错误、危险仅在改变颜色会造成业务误读时保留；除上述前三名保护范围外，其他排名编号和普通装饰不自动受保护。
- 默认保持盲测隔离。除非用户明确要求参考设计师版本，不得读取相邻已完成页面的截图或颜色。

## 四个必需执行产物

为减少重复字段，执行过程只维护四个顶层对象；参考文件定义其内容：

```text
sourceAudit    原稿结构、Paint、角色、灰度与显著性证据
themeDecision 新 KV 分析、配色提案、用户选定方案、最终色板和角色决策
mutationPlan  副本、节点白名单、Paint 配方、解绑与写入记录
finalAudit    结构差异、对比度、残留色、截图与最终 PASS/FAIL
```

字段可以存在于对象内部，但不要在多个文件中重复建立同义字段。大快照/截图通过路径与哈希引用；场景、门禁、批次和阶段日志统一归入 `mutationPlan`，每个实际动作后保存，支持中断恢复。

四个产物和所有下载图片、配色预览、快照、试色截图、派生视图、节点映射、批次与门禁 JSON 都是**本次运行的临时证据**，不得写入或复用 Skill 根目录中的稳定 `artifacts/`、`work-*.png` 或历史任务目录。Phase 0 必须用 `scripts/run_workspace.py init` 创建本次唯一 `runWorkspace`，后续旁文件只写入该目录；新任务禁止把旧目录中的 KV、截图、色值或 PASS 当成当前输入。只有用户明确要求恢复同一次未完成任务，且 `fileKey`、目标 ID、新 KV 身份/哈希和原稿指纹全部匹配时，才可复用该未完成目录。

Phase 7 通过并已提取完成回复所需的 Frame ID、位置、模式和最终结论后，默认执行 `scripts/run_workspace.py cleanup <runWorkspace>`，删除本次全部本地临时证据；Figma 中的原稿与完成副本不受影响。用户明确要求保留审计包或当前任务尚未完成时才暂不清理。清理只能针对脚本创建、带有效标记且位于系统临时根下的精确目录，禁止用通配符扫描或删除其他历史/用户文件。完成回复须报告 `runWorkspaceCleanup=pass|kept-by-user|not-complete`；清理失败不得伪装成已清理。

## 阶段与最终门禁

详细的 Phase 0–7 执行步骤、必须产物和通过条件只维护在[《局部试色与执行工具》第 6 节](references/execution-workflow.md#6-阶段产物与通过条件)。首次仍按依赖顺序执行；按当前阶段读取，不重新加载整份规则。

Phase 3 写入前冻结适用规则清单、全量颜色通道分类与视觉单元；Phase 5 必需代表场景逐条有证据通过后才扩展。Phase 7 必须实际重算全量覆盖、目标完成、原稿/保护/结构不变、同色关系和对应对比度/强弱顺序，并在最终新鲜快照上验证完整视觉单元及全部实例覆盖。任何 FAIL/UNKNOWN、漏项或过期证据都禁止报告完成，警告不能消除必需失败。工具和严格汇总见该文件第 5 节。

## 完成回复

默认简洁报告，不把全部内部字段倾倒给用户：

- 原 Frame 与副本 ID、位置，以及新 KV 是否替换成功。
- `sourceUiMode → targetUiMode`、`targetSurfaceDepth`、最终交互配色方向、按钮可见结构和颜色表现；只有存在可靠证据时才另记金属质感方向。
- Phase 0–7 的 `PASS/FAIL`，及带证据的警告/不适用项。
- 结构保护、文字阶梯、按钮对比度及合成色彩力度、媒体与前三名颜色保护、残留色和视觉审查结论。
- 解绑实例与跳过节点的数量及理由；若无则明确为 0。

只有 `finalAudit=pass` 或全部真实门禁通过且附有约束说明的 `pass-with-warnings` 时才可使用“已完成换肤”；后者必须同时说明警告，不声称满足未达标的无障碍标准。详细节点、候选与审计日志仅在任务运行中保留；成功完成后按上述规则清理。用户要求长期保留审计包时才把它作为显式交付物保存，并在回复中说明保存位置。
