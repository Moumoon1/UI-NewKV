# skill-palette-choice-v2

当前版换肤流程，特点：

- 先分别判断旧 KV、新 KV 和原 UI 的深浅，并从新 KV 提炼环境色、材质色、主视觉色相。
- 深色新 KV 默认提供深/浅 UI × 同色系/撞色 4 套方案；浅色新 KV 默认提供浅色 UI × 同色系/撞色 2 套方案。
- 同色系方案延续 KV 环境与材质家族；撞色方案由主视觉色相的对立色生成按钮、CTA 和高亮字。
- 用户选定方案前只读分析，不复制页面、不替换 KV、不写 UI；选定后自动完成试色、扩展和严格验收。
- 附带确定性色板预览脚本 `scripts/render_palette_proposals.py`，便于用统一角色顺序比较方案。
